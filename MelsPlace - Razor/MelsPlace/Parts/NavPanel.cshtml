<div ng-repeat="button in navPages" class="navBarBtnContainer">
    <input type="button" class="button navBtn" ng-class="{active: button.Active}" ng-click="goTo(button)" value="{{button.Text}}"
           onclick="this.blur()" />
</div>